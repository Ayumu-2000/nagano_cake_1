<h1>商品一覧</h1>
<% @items.each do |item| %>
    <%= image_tag item.get_item_image(100,100) %>
    <!--商品id-->

    <% link_to admin_item_path(item.id) do %>
        <%= item.name %>
    <% end %>
    <%= item.price %>
<% end %>